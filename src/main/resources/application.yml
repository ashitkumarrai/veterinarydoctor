spring:
    datasource:
        password: 123456789
        url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/veterinarydrappointment
        username: root
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true


    main:
        banner-mode: off
    output:
        ansi:
          enabled: ALWAYS

